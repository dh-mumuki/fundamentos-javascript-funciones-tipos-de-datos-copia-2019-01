describe("", function() {
  it("Declarada la variable edad", function() {
    assert.notEqual(edad, "undefined");
  });
    it("Declarada la variable peso", function() {
    assert.notEqual(peso, "undefined");
  });
})